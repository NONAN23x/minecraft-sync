[paths]
# Set the paths for source (Leave it empty in most cases to auto-detect)
source_base = 
# Set this to the base path of your Minecraft installation (auto-detect if blank)
minecraft_path =

[folders]
# Enable or disable syncing for each folder type
# Accepted Values: true or false
sync_mods = true
sync_resourcepacks = true
sync_shaderpacks = true